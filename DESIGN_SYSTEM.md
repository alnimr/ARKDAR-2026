# ARKDAR Digital Design System (Elite DNA)

هذا المستند هو المرجع الوحيد والنهائي للهوية البصرية لمنصة ARKDAR. أي إضافة برمجية مستقبلية يجب أن تلتزم بهذه "التوكنز" (Design Tokens) لضمان استمرارية الروح النخبوية للمشروع.

## 1. رموز التصميم (Design Tokens) 🛡️

يتم تعريف هذه الرموز في `globals.css` تحت مسمى `arkdar` لضمان عدم التداخل:
- **Arkdar Royal**: `#840505` (اللون السيادي لآركدار، يُستخدم في الحدود، الوهج، والعناصر الأساسية).
- **Arkdar Oxblood**: `#660000` (الدرجة العميقة للتدرجات).
- **Arkdar Carmine**: `#911010` (درجة التباين للنصوص التفاعلية والعناوين).
- **Surface light**: `#EDF2F4` (خلفية الوضع الفاتح).
- **Surface Dark**: `#0A0A0A` (خلفية الوضع الداكن).

## 2. المعايير التفاعلية (Interactive Standards) ⚡

### التدرج اللوني الأساسي (The Master Gradient)
كافة الأزرار والعناصر التفاعلية الرئيسية يجب أن تستخدم التدرج التالي حصراً:
```css
background: linear-gradient(135deg, var(--color-arkdar-carmine), var(--color-arkdar-oxblood));
```

### البطاقات والنوافذ (Cards & Modals)
تتبع كافة الحاويات التفاعلية نظام "الشفافية النخبوية":
- **الحدود**: يجب أن تكون دائماً `border-arkdar-royal/20` (أو `/30` في الوضع الداكن).
- **التأثير**: `backdrop-blur-xl` كحد أدنى.
- **الظلال**: استخدام الوهج (`var(--primary-glow)`) المعتمد على اللون الكرموزي.

## 3. تجربة المستخدم واستمرارية البيانات (UX DNA) 🧠

### استمرارية التفضيلات (Persistence)
يجب الحفاظ على دقة 100% في تذكر تضيلات الزوار عبر الكوكيز:
- **NEXT_LOCALE**: حفظ اللغة المختارة.
- **arkdar_modal_dismissed**: حالة نافذة الترحيب.
- **theme**: الوضع اللوني (عبر next-themes).

### التدويل (i18n Fallback)
- **اللغة الافتراضية**: هي الإنجليزية (`en`) في حال فشل الاكتشاف التلقائي.
- **شاشة الترحيب**: تظهر دائماً في الزيارة الأولى إذا لم يتم ضبط اللغة مسبقاً لمساعدة المستخدم على الاختيار.

## 4. القواعد البرمجية (Engineering Rules) 📐

- **ممنوع** استخدام ألوان ثابتة (Hex Codes) داخل المكونات. يجب الرجوع دائماً لمتغيرات CSS المتوفرة في `globals.css`.
- **التوافق**: يجب التأكد من تباين النصوص (Burnt Carmine للعناوين في الفاتح، والرمادي الفاتح للنصوص في الداكن).

> [!IMPORTANT]
> هذا التصميم يمثل "الحمض النووي" للموقع؛ أي تغيير في الألوان الأساسية يجب أن يتم مركزياً في `globals.css` لتموج التغيير عبر المنصة بالكامل.
