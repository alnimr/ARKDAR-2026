# مهمة استخراج الديوان الشامل (Universal Data Extraction)

## الهدف
استخراج كافة المقالات من 5 قواعد بيانات (ملفات SQL) موجودة في المسار الخارجي الذي سأحدده لك، ودمجها في `src/data/articles.json`.

## التعليمات (Data Agent Instructions)
1. **الوصول الخارجي**: اطلب مني المسار الكامل لمجلد قواعد البيانات (خارج مجلد المشروع).
2. **المسح الشامل**: ابحث في ملفات الـ SQL الخمسة عن أي مدخلات في جدول `posts`.
3. **قواعد الاستخراج**:
   - استخراج جميع المقالات بغض النظر عن اللغة أو حالة النشر (Published/Draft).
   - سحب المحتوى الكامل `post_content` مع تنظيفه من أكواد Elementor.
   - تحويل الروابط (Slugs) إلى ASCII (الإنجليزية) لضمان توافق Vercel.
4. **المخرجات**:
   - تحديث ملف `src/data/articles.json`.
   - إضافة حقل `source_db` لكل مقال يوضح اسم الملف الأصلي.
5. **إحصائية قبل التنفيذ**: قدم لي تقريراً بعدد المقالات المكتشفة في كل ملف قبل دمجها.