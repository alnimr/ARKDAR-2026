# خريطة السيادة البصرية (ARKDAR Sovereign Map)

هذا الملف هو المرجع التقني والمكاني لكل ما يخص الهوية البصرية الملكية (Royal DNA v3.1) داخل المشروع. يُستخدم لتسريع البحث وضمان عدم حدوث أي انحراف عن الهوية.

## 1. الثوابت والقيم الأساسية (Tokens)
- **ملف المتغيرات الرئيسي:** `src/app/globals.css` (تحت قسم `:root`)
- **ملف الرموز البرمجية (JS/TS):** `src/styles/theme.ts`
- **القواعد الصارمة:**
    - الزوايا (Radii): `0px` دائماً.
    - الشبكة (Grid): 12 عموداً، فجوة `24px`.
    - الوحدة الأساسية (Base Unit): `8px`.

## 2. الهندسة والزخارف (Ornaments)
- **المكون الرئيسي:** `src/components/core/MamlukOrnaments.tsx`
- **المعادلات الرياضية:** مستوحاة من `arkdar_ornaments_advanced.html`.
- **الطبقات (Layers):** `z-0` للخلفيات الزخرفية، `z-10` للمحتوى.

## 3. الخطوط والطباعة (Typography)
- **توزيع الأدوار:**
    - **El Messiri:** للغة العربية (جميع العناوين والنصوص).
    - **Cinzel:** للغة الإنجليزية (العناوين الفرعية، الأرقام، Caps).
    - **Scheherazade New:** لنصوص المقالات الطويلة (Body Copy).
- **الضبط:** `letter-spacing` ملغى تماماً للعربي.

## 4. نظام الأيقونات (Icons)
- **المجلد:** `src/components/icons/`
- **المعيار:** Level 2 (Calligraphic / Variable Weight).
- **المكتبة:** تستخدم أيقونات SVG مخصصة مبنية على النسبة الذهبية.

## 5. الألوان والعمق (Depth)
- **الفلسفة:** استبدال Glassmorphism بنظام الطبقات (`--l1` إلى `--l4`).
- **الظلال:** معرفة في `globals.css` تحت اسم `--depth-card` و `--depth-float`.

## 6. تخطيط الصفحات (Layout)
- **الحاويات:**
    - `max-w-[1280px]` (السعة القصوى).
    - `max-w-[960px]` (عرض المحتوى القياسي).
    - `max-w-[720px]` (العرض الذهبي للقراءة).

---
**تنبيه:** أي كود يحتوي على `rounded-` أو `backdrop-blur` يُعتبر "ديناً تقنياً" (Legacy Debt) ويجب تصحيحه فوراً.
